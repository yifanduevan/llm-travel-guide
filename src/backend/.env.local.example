# Local backend-only environment variables (do not commit real secrets).
# Copy this file to src/backend/.env.local and fill your values.

# LLM key resolution mode for local testing.
LLM_SECRET_PROVIDER=env

# Local testing key only. Keep this out of git.
OPENAI_API_KEY=

# Optional local DB overrides
# DB_URL=jdbc:postgresql://127.0.0.1:5432/ece651?stringtype=unspecified
# DB_USER=ece651
# DB_PASSWORD=password
