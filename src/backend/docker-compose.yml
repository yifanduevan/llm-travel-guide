services:
  db:
    image: postgres:16
    container_name: ece651_db
    environment:
      POSTGRES_DB: ece651
      POSTGRES_USER: ece651
      POSTGRES_PASSWORD: password
    ports:
      - "5433:5432"
    volumes:
      - ece651_pgdata:/var/lib/postgresql/data
    

volumes:
  ece651_pgdata:
